public class TerminalNode extends Node{
    TerminalNode(String _value){
        sValue = _value;
        //value = Double.parseDouble(sValue);
    }
    /*public double getValue(String [] obj) {
        //System.out.println(sValue);
        // if x then return val[1]...
        switch (sValue)
        {
            case "x1":
                return Double.parseDouble(obj.x1);

            case "x2":
                return Double.parseDouble(obj.x2);

            case "x3":
                return Double.parseDouble(obj.x3);

            case "x4":
                return Double.parseDouble(obj.x4);

            case "x5":
                return Double.parseDouble(obj.x5);

            default:
                return 1.0;

        }
    }*/
}
